{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "serve": "npx serve .output/public",
    "genserve": "nuxt generate && npx serve .output/public"
  },
  "devDependencies": {
    "@nuxt/content": "^2.8.3",
    "nuxt": "^3.7.4"
  }
}
